<!DOCTYPE html>
<html>
  <head>
    <title>MovieApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%# bootstrapの機能を実装するためのコード %>
     <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/r29/html5.min.js">
      </script>
    <![endif]-->
  </head>
  <header class="navbar navbar-fixed-top navbar-inverse">
        <%= link_to "MOVIE TALK", root_path, id:"logo"%>
          <nav>
            <ul class="nav nav-pills ">
              <% if logged_in? %>
                <li class="nav-item"><%= link_to "MOVIE TALKについて", about_path %></li>
                <li class="nav-item"><%= link_to "Showページ", user_path(current_user) %></li>
                <li class="nav-item"><%= link_to "入場者一覧", users_path %></li>
                <li class="nav-item"><%= link_to "ユーザー情報編集", edit_user_path(current_user) %></li>
                <li class="nav-item"><%= link_to "Log out", logout_path, data: { turbo_method: :delete } %></li>
              <% else %>
                <li class="nav-item"><%= link_to "MOVIE TALKについて", about_path %></li>
                <li class="nav-item"><%= link_to "入館", login_path %></li>
                <li class="nav-item"><%= link_to "新規登録", new_user_path %></li>
              <% end %>
            </ul>
          </nav>
    </header>

  <body>
    <div id="main-message">
      <div class="container">
        <% flash.each do |message_type,msg|%>
          <div class="alert alert-<%=message_type%>"><%= msg%></div>
        <% end %>
      </div>
    </div>
    <div class = "container">
    <%= yield %>
    <%= debug(params) if Rails.env.development? %>
    </div>

     <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>
  </body>
  
</html>

