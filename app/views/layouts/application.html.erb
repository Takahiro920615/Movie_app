<!DOCTYPE html>
<html>
  <head>
    <title>MovieApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/r29/html5.min.js">
      </script>
    <![endif]-->
  </head>
  <header class="navbar navbar-fixed-top navbar-inverse">
        <%= link_to "MOVIE TALK", root_path, id:"logo"%>
          <nav>
            <ul class="nav navbar-nav navbar-right">
             <% if logged_in? %>
               <li><%= link_to "MOVIE TALKについて", about_path%><li>
               <li><%= link_to "Showページ",user_path(current_user)%></li>
               <li><%= link_to"入場者一覧",users_path%></li>
               <li><%= link_to"ユーザー情報編集",edit_user_path(current_user)%></li>
               <li><%= link_to "Log out", logout_path, method: :delete, data: { confirm: "Are you sure?" } %></li>
             <% else%>
               <li><%= link_to "MOVIE TALKについて", about_path%><li>
               <li><%= link_to "入館",login_path%></li>
               <li><%= link_to "新規登録",new_user_path%></li>
             <% end%>
           </ul>
          </nav>
    </header>

  <body>
    <div id="main-message">
      <div class="container">
        <% flash.each do |message_type,msg|%>
          <div class="alert alert-<%=message_type%>"><%= msg%></div>
        <% end %>
      </div>
    </div>
    <div class = "container">
    <%= yield %>
    <%= debug(params) if Rails.env.development? %>
    </div>
  </body>
  
</html>

